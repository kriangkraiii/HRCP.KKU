-- V28: ความเห็นที่ผู้ลงนามพิมพ์เองตอนพิจารณา
--
-- ก่อนหน้านี้ผู้ลงนามพิมพ์อะไรได้ที่เดียวคือ decline_reason ซึ่งอยู่บนเส้นทางปฏิเสธ พิมพ์เมื่อไหร่
-- การเวียนก็จบเมื่อนั้น คนที่เห็นด้วยแต่อยากฝากข้อสังเกตไว้จึงไม่มีที่ให้เขียน
--
-- อยู่ที่ signature_step ด้วยเหตุผลเดียวกับ signer_choice_value (ดู V26): ซองลายเซ็น freeze
-- เนื้อหาเอกสารไว้ด้วย frozen_hash การเขียนความเห็นลง json_data หลังเริ่มเวียนจะทำให้แฮชไม่ตรง
-- แล้ว SignatureWorkflowService.sign ประกาศให้ลายเซ็นทั้งซองเป็นโมฆะ ความเห็นจึงถูกเติมเข้า
-- เอกสารตอน render เท่านั้น เหมือนชื่อผู้ลงนามและคำตอบของช่องที่มีคำถาม
--
-- 1000 ตัวอักษรเท่ากับ signature_audit_event.detail เพราะความเห็นถูกต่อท้าย audit detail ด้วย
-- เขียนทั้งสองเส้นทาง (เห็นควร / ไม่เห็นควร) ส่วน decline_reason ยังเขียนเหมือนเดิมทุกประการ
-- เพื่อให้ notifyDeclined และแถวเก่าที่มีอยู่แล้วทำงานต่อได้โดยไม่ต้องย้ายข้อมูล

ALTER TABLE signature_step ADD COLUMN IF NOT EXISTS signer_comment VARCHAR(1000);
